package ${packageName}

import ${packagePresentationName}.util.SingleEvent
import ${packagePresentationName}.util.ViewError

data class ${className}(
        val data: Any = Any(),
        val viewError: SingleEvent<ViewError>? = null,
        val isLoading: Boolean = false
)
