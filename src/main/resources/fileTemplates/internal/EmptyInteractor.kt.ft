package ${packageName}

import javax.inject.Inject
import ${packageDomainName}.domain.interactors.EmptyInteractor

class ${className} @Inject constructor(): EmptyInteractor {

    override suspend fun invoke(input: Unit) {
        TODO()
    }
}
