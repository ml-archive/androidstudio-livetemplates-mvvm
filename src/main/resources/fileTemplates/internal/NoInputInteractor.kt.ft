package ${packageName}

import javax.inject.Inject
import ${packageDomainName}.domain.interactors.NoInputInteractor

class ${className} @Inject constructor(): NoInputInteractor<${outputType}> {

    override suspend fun invoke(input: Unit): ${outputType} {
        TODO()
    }
}
