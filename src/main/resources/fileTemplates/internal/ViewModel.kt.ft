package ${packageName}

import ${packagePresentationName}.ui.base.BaseViewModel
import javax.inject.Inject

class ${className} @Inject constructor() : BaseViewModel<${vsClass}>() {

    override val initState: ${vsClass} = ${vsClass}()
}
