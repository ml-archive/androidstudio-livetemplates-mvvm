package ${packageName}

import javax.inject.Inject
import ${packageDomainName}.domain.interactors.BaseAsyncInteractor

class ${className} @Inject constructor(): Interactor<${inputType}, ${outputType}> {

    override suspend fun invoke(): ${outputType} {
        TODO()
    }
}
