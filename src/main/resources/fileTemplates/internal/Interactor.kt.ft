package ${packageName}

import javax.inject.Inject
import ${packageDomainName}.domain.interactors.BaseAsyncInteractor

class ${className} @Inject constructor(): BaseAsyncInteractor<${returnType}> {

    override suspend fun invoke(): ${returnType} {
        TODO()
    }
}
