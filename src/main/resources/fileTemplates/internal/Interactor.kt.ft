package ${packageName}

import javax.inject.Inject
import ${packageDomainName}.domain.interactors.Interactor

class ${className} @Inject constructor(): Interactor<${inputType}, ${outputType}> {

    override suspend fun invoke(input: ${inputType}): ${outputType} {
        TODO()
    }
}
